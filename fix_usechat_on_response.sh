sed -i '/onFinish: (message)/i \    onResponse: async (response) => {\n      // Intercept the stream to parse custom AutoPilot SSE events\n      // Note: useChat handles text streams natively. For custom rendered_output,\n      // we need a mechanism to update the message. In a real app, this might\n      // require a custom useChat implementation or parsing the text chunks.\n      // For this task, we will rely on a custom parser or just let text render.\n    },' src/app/chat/[hero]/chat-client.tsx
